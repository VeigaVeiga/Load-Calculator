Load-Calculator V25 — Packing Engine 2.0

# Railway Container Loading Planner

React + TypeScript + Three.js loading planner for 20GP, 40GP, 40HQ and custom containers.

## Build

```bash
npm install
npm run build
```

The Vite production output is generated in `dist/` and is intentionally ignored by Git.
